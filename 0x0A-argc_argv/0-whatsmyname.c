#include <stdio.h>
/**
 * main - program that prints its name, f by a nl
 * @argc: nmber of arguments
 * @argv: array of argc's size
 * Return: 0
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
