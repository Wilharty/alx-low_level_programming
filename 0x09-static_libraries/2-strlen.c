#include "main.h"
/**
 * _strlen - funct that returns the length of a string
 * @s: string variable
 * Return: the length
 */
int _strlen(char *s)
{
	int l;

	while
		(*s != '\0')
	{
		l = l + 1;
	}
	return (l);
}
