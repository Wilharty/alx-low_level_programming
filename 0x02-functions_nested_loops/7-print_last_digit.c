#include "main.h"
/**
 * print_last_digit - funct that print the last digit of a nmber
 * @n: number
 * Return: l if success
 */
int print_last_digit(int n)
{
	l = n % 10
	if (l != 0)
	{
		return (l);
	}
	else
	{
		return (0);
	}
}
