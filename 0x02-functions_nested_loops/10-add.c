#include "main.h"
/**
 * add - funct that adds 2 int and return the result
 * @a: first integer
 * @b: second interger
 * Return: sum if success
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
