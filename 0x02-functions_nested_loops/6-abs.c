#include "main.h"
/**
 * _abs - funct that computes the abs path val of an int
 * @i: integer variable
 * Return: 0 if success
 */
int _abs(int i)
{
	if (i < 0)
	{
		i = -i
		_putchar('i');
	}
	else
	{
		_putchar('i');
	}
		_putchar('\n');
	return (0);
}
