#include "main.h"
/**
 * print_numbers - funct that prints nmbers, 0 to 9, f by a nl
 * Return: 0 if success
 */
void print_numbers(void)
{
	int i;

	for
		(i = '0', i <= '9', i++)
	{
		_putchar(i);
	}
		_putchar('\n');
}
