#include <stdio.h>
/**
 * main - prog that print the alph in lwcase, f by a new l
 * Return: Always 0 (Success)
*/
int main(void)
{
	char low = 'a';

	while
		(low <= 'z')
		{
			putchar(low);
			low++;
		}
			putchar('\n');

	return (0);
}
