#include "main.h"
/**
 * _strlen - funct that returns the length of a string
 * @s: string variable
 * Return: the length
 */
int _strlen(char *s)
{
	int l = 0;

	while
		(*s++)
	{
		l++;
		return (l);
	}
}
